<h1>It works</h1>
<% @program.sessions.each do |session| %>
  <div class="session">
    <h2>Session on <%= session.date %></h2>
    <p>Type: <%= session.session_type %></p>
    <p>Duration: <%= session.duration_min %> minutes</p>
<% end %>
